name: Create Distributable
on:
    workflow_dispatch:
    push:
        branches:
            - release/*-distr*

jobs:
    checkout:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo content
              uses: actions/checkout@v5
